# Superpowers planning scratch (brainstorming/writing-plans design notes).
# These are throwaway per-task artifacts, not shippable docs — keep them local
# so they never leak into history the way the doc-sync-init spec once did.
docs/superpowers/
